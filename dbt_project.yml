name: br_credit_monitor
version: "0.1.0"
config-version: 2
profile: br_credit_monitor

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  br_credit_monitor:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
